{
  "name": "@openhuman/api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "uv run ruff check app/",
    "generate:spec": "uv run python scripts/export_openapi.py"
  }
}
